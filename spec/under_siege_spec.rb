require 'spec_helper'

describe UnderSiege do
  it 'has a version number' do
    expect(UnderSiege::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
